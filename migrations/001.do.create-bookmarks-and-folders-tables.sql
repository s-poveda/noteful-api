
CREATE TABLE folders (
	id INTEGER GENERATED BY DEFAULT AS IDENTITY,
	title TEXT NOT NULL,
);

CREATE TABLE notes (
	id INTEGER GENERATED BY DEFAULT AS IDENTITY,
	title text NOT NULL,
	content text DEFAULT NULL,
	folder_id REFERENCES folders(id) NOT NULL
);
